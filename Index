<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Dark Mode Index Page</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #111;
            color: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            overflow: hidden;
        }

        #container {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            font-size: 2em;
            margin-bottom: 10px;
        }

        a {
            color: #3498db;
            text-decoration: none;
            transition: color 0.3s ease-in-out;
        }

        a:hover {
            color: #2980b9;
        }

        .icon {
            font-size: 2em;
            margin: 10px;
            transition: transform 0.3s ease-in-out;
        }

        .icon:hover {
            transform: scale(1.2);
        }

        @media (prefers-reduced-motion: reduce) {
            .icon, a:hover {
                transition: none;
            }
        }
    </style>
</head>
<body>
    <div id="container">
        <h1>Your Name</h1>
        <a href="#" class="icon">📷</a>
        <a href="#" class="icon">📧</a>
        <a href="#" class="icon">🌐</a>
        <!-- Add more links or icons as needed -->
    </div>
</body>
</html>
